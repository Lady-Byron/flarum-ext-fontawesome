{
  "extends": "flarum-tsconfig",

  "include": [
    "src/**/*",
    "../vendor/*/*/js/dist-typings/@types/**/*",
    "node_modules/@types/**/*",
    "@types/**/*"
  ],

  "compilerOptions": {
    "baseUrl": ".",
    "declarationDir": "./dist-typings",

    // 让第三方类型也能被解析到
    "typeRoots": [
      "./node_modules/@types",
      "../vendor/flarum/core/js/dist-typings/@types"
    ],
    "types": ["node", "mithril", "dayjs"],

    "moduleResolution": "Node",
    "skipLibCheck": true,

    "paths": {
      // 核心：flarum/* -> vendor/flarum/core/...
      "flarum/*": ["../vendor/flarum/core/js/dist-typings/*"]

      // 如需引用其它官方扩展的类型，按需再各自添加一行（每行只用一个 *）
      // 例如：
      // "flarum/tags/*": ["../vendor/flarum/tags/js/dist-typings/*"],
      // "flarum/likes/*": ["../vendor/flarum/likes/js/dist-typings/*"]
    }
  }
}
